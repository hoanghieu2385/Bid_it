src/
├── assets/                     # Static assets like images, fonts, custom stylesheets
│   ├── images/                 # Image resources (e.g., logos, icons)
│   ├── styles/                 # Global and scoped CSS
│   └── react.svg               # Sample SVG asset
│
├── components/                # Reusable UI components (not tied to routing)
│   ├── admin/                 # Admin-specific UI components
│   ├── client/                # Client-specific UI components
│   │   ├── home/              # UI components used on the Home page
│   │   ├── profile/           # Components related to user profile
│   └── common/                # Shared UI components used by both client and admin
│
├── contexts/                  # React context providers (e.g., Auth, Theme)
│
├── hooks/                     # Custom React hooks
│
├── layout/                    # Layout wrappers for client/admin pages (header, sidebar, etc.)
│
├── pages/                     # Main route-based page components
│   ├── admin/                 # All admin-side pages
│   ├── client/                # All client-side pages
│   │   ├── auction/           # Auction-related pages (create, detail, edit)
│   └── common/                # Pages shared between admin and client (if any)
│
├── routes/                    # Route configuration for client/admin
│   ├── ClientRoutes.jsx       # React Router routes for client side
│   └── AdminRoutes.jsx        # React Router routes for admin side
│
├── services/                  # API service modules to interact with backend
│
├── templates/                 # Static HTML templates (e.g., email or verification)
│
├── utils/                     # Helper functions and utilities
│
├── App.jsx                    # Main React App component
├── main.js                    # Entry point for Vite + React
└── index.css                  # Global CSS (including Bootstrap import)
