package com.example.auction.model;

public enum AuctionStatus {
    UPCOMING,
    OPENED,
    CANCELLED,
    CLOSED,
    SOLD,
    FAILED,
    SHIPPING,
    DELIVERED,
    DISPUTED,
    PENDING_RETURN,
    RETURNING,
    COMPLETED
}