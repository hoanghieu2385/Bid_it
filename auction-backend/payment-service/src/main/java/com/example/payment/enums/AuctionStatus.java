package com.example.payment.enums;

public enum AuctionStatus {
    UPCOMING,
    OPENED,
    CANCELLED,
    CLOSED,
    SOLD,
    FAILED,
    SHIPPING,
    DELIVERED,
    DISPUTED,
    PENDING_RETURN,
    RETURNING,
    COMPLETED
}
